# Passage — уведомления в WhatsApp по UID с локальными изображениями

Проект принимает события «визита» по UID, логирует их в Postgres и шлёт уведомления в WhatsApp.  
Если карточка UID найдена, бот отправляет **локальное изображение** из папки `images` с подписью; если нет — сообщает о неизвестном UID.

---

## 📁 Структура

```
.
├─ images/                  # Локальные фото (например: 04_A2_3B_1C.jpg)
├─ src/
│  ├─ db.ts                 # Подключение к Postgres (postgres.js)
│  ├─ index.ts              # Точка входа (инициализация WA + HTTP)
│  ├─ server.ts             # HTTP API (/health, /visit)
│  └─ wa.ts                 # Клиент whatsapp-web.js
├─ Dockerfile
├─ bunfig.toml / bun.lockb
├─ package.json
└─ README.md
```

---

## ⚙️ Зависимости

- **Node.js** 20+
- **Bun** 1.3+ (запуск и установка пакетов)
- **PostgreSQL** 13+
- **Chromium/Chrome** для `whatsapp-web.js` (через Puppeteer)

> В контейнере рекомендуется ставить системный `chromium` и **передавать путь** через переменную `PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium` (см. ниже).

---

## 🔧 Переменные окружения

| Переменная | Обязательно | Пример | Назначение |
|---|:---:|---|---|
| `DATABASE_URL` | ✅ | `postgres://user:pass@host:5432/db` | Подключение к БД |
| `PORT` | ❌ | `3000` | Порт HTTP-сервера (по умолчанию `3000`) |
| `WHATSAPP_JID` | ✅ | `1234567890@c.us` | Получатель уведомлений |
| `API_KEY` | ✅ | `secret` | Ключ для авторизации HTTP‑запросов |
| `IMAGES_DIR` | ❌ | `./images` | Локальная папка с картинками |
| `PUPPETEER_EXECUTABLE_PATH` | ❌ | `/usr/bin/chromium` | Путь к Chromium/Chrome в контейнере |
| `PUPPETEER_SKIP_DOWNLOAD` | ❌ | `1` | Если используете системный Chromium |

Bun автоматически подхватывает `.env` в корне проекта.

---

## 🗄️ Схема БД

```sql
CREATE TABLE IF NOT EXISTS cards (
  id         BIGSERIAL PRIMARY KEY,
  uid        TEXT UNIQUE NOT NULL,
  image      TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS visits (
  id        BIGSERIAL PRIMARY KEY,
  card_id   BIGINT REFERENCES cards(id) ON DELETE CASCADE,
  uid       TEXT NOT NULL,
  seen_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_cards_uid   ON cards(uid);
CREATE INDEX IF NOT EXISTS idx_visits_time ON visits(seen_at DESC);
```

> Если `cards.image` пусто, имя файла берётся как `uid` с заменой `:` на `_` и добавлением `.jpg`.  
> Пример: UID `AA:BB:CC:DD` → файл `AA_BB_CC_DD.jpg` в папке `images`.

---

## ▶️ Локальный запуск

```bash
# 1) Установить зависимости
bun install

# 2) Применить миграции (вручную psql/pgcli или через любимый мигратор)
#    psql "$DATABASE_URL" -f schema.sql

# 3) Запустить
bun run src/index.ts
```

После первого старта в консоли появится QR‑код — отсканируйте его в WhatsApp на телефоне.  
При успешной авторизации вы увидите `WhatsApp ready` и строки:

```
HTTP listening on http://localhost:3000
Images dir: ./images
```

---

## 🐳 Запуск в Docker (рекомендуемо)

**Ключевая идея**: ставим системный `chromium` в образ, отключаем автоскачивание Puppeteer и передаём путь через env.

Пример **Dockerfile** (суть):
```dockerfile
FROM node:20 as base
RUN curl -fsSL https://bun.sh/install | bash
ENV PATH="/root/.bun/bin:$PATH"

RUN apt-get update && apt-get install -y chromium \
    libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 \
    libxcomposite1 libxrandr2 libxdamage1 libxkbcommon0 \
    libpango-1.0-0 libgbm1 libasound2 libatspi2.0-0 \
    libpangocairo-1.0-0 libxshmfence1 libxfixes3 \
    libwayland-client0 libwayland-cursor0 libwayland-egl1 \
    xdg-utils fonts-liberation && rm -rf /var/lib/apt/lists/*

ENV PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
ENV PUPPETEER_SKIP_DOWNLOAD=1

WORKDIR /app
COPY . .
RUN bun install
CMD ["bun", "run", "src/index.ts"]
```

Сборка и запуск:
```bash
docker build -t passage .

docker run --rm -it \
  -e DATABASE_URL="postgres://user:pass@host:5432/db" \
  -e API_KEY="secret" \
  -e WHATSAPP_JID="1234567890@c.us" \
  -e IMAGES_DIR="/app/images" \
  -e PUPPETEER_EXECUTABLE_PATH="/usr/bin/chromium" \
  -p 3000:3000 \
  -v $(pwd)/images:/app/images \
  -v passage-auth:/app/.wwebjs_auth \  # чтобы не логиниться каждый раз
  passage
```

> Если хотите, чтобы Puppeteer скачивал свой браузер сам, замените зависимость на `puppeteer` (не `puppeteer-core`) и **не** задавайте `PUPPETEER_SKIP_DOWNLOAD`. В контейнере это обычно тяжелее и дольше.

---

## 🔌 HTTP API

### Авторизация
Каждый запрос должен содержать заголовок:
```
x-api-key: <API_KEY>
```

### `GET /health`
Проверка живости сервиса. **200 OK** и текст `ok`.

### `POST /visit`
Создать запись посещения и отправить уведомление в WhatsApp.

**Тело запроса**:
```json
{ "uid": "AA:BB:CC:DD" }
```

**Ответы**:
- `200 {"ok": true, "unknown": true}` — UID не найден в `cards`, отправлено текстовое уведомление «Unknown UID».
- `200 {"ok": true, "image": "missing"}` — карточка найдена, но файл изображения отсутствует; отправлено текстовое уведомление.
- `200 {"ok": true, "file": "AA_BB_CC_DD.jpg"}` — изображение найдено и отправлено.
- `400` — некорректный JSON или отсутствует `uid`.
- `401` — неверный `x-api-key`.
- `404` — другой путь.

**Примечания**:
- При наличии записи в `cards` лог пишется в `visits(card_id, uid)`; иначе — `visits(uid)` без `card_id`.
- Подпись к фото: `UID: <uid>\nTime: <ISO>`.

---

## 🖼️ Правила именования изображений

1. Если в таблице `cards.image` задано имя файла — используется оно (как есть).
2. Если не задано — имя строится из `uid`: двоеточия заменяются на подчёркивания, добавляется `.jpg`  
   Пример: `12:34:56:78` → `12_34_56_78.jpg`.

Папка берётся из `IMAGES_DIR` (по умолчанию `./images`).

---

## 🔐 Безопасность и эксплуатация

- Храните `API_KEY` в секретах/переменных окружения.
- Ограничьте доступ к `/visit` (файрвол, allow‑list источников).
- Смонтируйте том для `.wwebjs_auth`, иначе после перезапуска потребуется заново сканировать QR.
- Логи WhatsApp не содержат тело сообщений, но содержат метаданные авторизации — не публикуйте их.

---

## 🛠️ Отладка и частые ошибки

- **`Could not find expected browser (chrome) locally...`**  
  Используете `puppeteer-core` без установленного браузера.  
  Решение: установить `chromium` и задать `PUPPETEER_EXECUTABLE_PATH`, либо перейти на пакет `puppeteer`.

- **Namespace/Sandbox ошибки в контейнере**  
  Добавьте флаги: `--no-sandbox --disable-setuid-sandbox` (в проекте уже указаны).

- **Нет изображения**  
  Проверьте, какое имя ожидается (лог показывает `expected: <file>`), и что файл есть в `IMAGES_DIR`.

- **Нет отправки в WhatsApp**  
  Дождитесь события `WhatsApp ready`. Проверьте, что телефон онлайн и не «забанил» сессию.

---

## ✅ Быстрый чек‑лист

- [ ] Настроен `DATABASE_URL`, выполнены миграции.
- [ ] Заполнена таблица `cards` (UID и, при необходимости, `image`).
- [ ] Папка `images` содержит нужные файлы с корректными именами.
- [ ] Указан `WHATSAPP_JID` и отсканирован QR.
- [ ] Работает `/health`, авторизация по `x-api-key`.
- [ ] В Docker: установлен `chromium`, передан `PUPPETEER_EXECUTABLE_PATH`, смонтирован `.wwebjs_auth`.
